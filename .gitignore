# fetched data lives in the `latest-data` github release, not in git — `make` it
# locally, or the deploy workflow downloads it into the pages artifact.
data/
# browser automation artifacts
.playwright-mcp/
__pycache__/
*.pyc
.DS_Store
.env
.venv/
*.egg-info/
